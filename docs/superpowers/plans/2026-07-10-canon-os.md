# Canon OS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Canon OS to Brand OS: folder-per-entity dossiers (`static/brand/canon/<slug>/manifest.json` + co-located reference images) that the canonical build script validates and compiles into paste-verbatim prompt descriptors in brand.txt.

**Architecture:** A new stdlib-only module `scripts/canon.mjs` (discovery, validation, compilation as pure functions) imported by `scripts/build-brand-txt.mjs`. Entities auto-discovered under `brand.json.canon.root`. Validation failures kill the build per-entity and loudly. Backward compatible: the existing flat `characters` emitter is untouched.

**Tech Stack:** Node stdlib only (hard constraint inherited from build-brand-txt.mjs — no npm deps). Tests via `node --test`. Approved spec: `~/Documents/github-repos/garyinparadise/docs/strategic-documents/2026-07-10-canon-os-design.md`.

## Global Constraints

- `scripts/canon.mjs` and all changes to `build-brand-txt.mjs` use Node stdlib ONLY (`node:fs`, `node:path`) — the script must run in any build environment with zero installs.
- Entity `type` is exactly one of `person | place | prop`.
- The brand.txt section heading is exactly: `## Canon (compiled descriptors — paste verbatim, never paraphrase)`.
- Old flat `characters` blocks and loose GABR files remain valid: no removals, no behavior changes to existing emitters.
- Repos touched: `~/Documents/github-repos/supersuit-repos/brand-os-template` (Tasks 1–4), `~/.claude/skills/*` (Task 5, not a git repo — no commits), `~/Documents/github-repos/nation-of-fire/wheel-and-oar-book` (Task 6), `~/Documents/github-repos/nation-of-fire/universe` (Task 7).
- Template commits (Tasks 1–4) end with:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- Book/universe commits (Tasks 6–7) carry NO co-author trailer (Gary's standing rule for Nation of Fire repos).
- Gary's approval rules are canon: manifests encode that only Gary relocks references (`authority` block).

---

### Task 1: `scripts/canon.mjs` — discovery + validation

**Files:**
- Create: `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon.mjs`
- Create: `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon.test.mjs`
- Create: test fixtures under `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/test-fixtures/canon/`
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/package.json` (add `"test": "node --test scripts/"`)

**Interfaces:**
- Produces: `discoverEntities(canonRootAbs: string) -> Array<{dir, slug, manifest, parseError}>`; `validateManifest(entity) -> string[]` (empty array = valid). Task 2 adds `compileEntity` to this same module; Task 3 imports all three.

- [ ] **Step 1: Create test fixtures**

```bash
cd ~/Documents/github-repos/supersuit-repos/brand-os-template/scripts
mkdir -p test-fixtures/canon/valid-person test-fixtures/canon/broken-entity
touch test-fixtures/canon/valid-person/front.png test-fixtures/canon/valid-person/turnaround.png
```

Write `test-fixtures/canon/valid-person/manifest.json`:

```json
{
  "slug": "valid-person",
  "type": "person",
  "name": "Testy",
  "role": "fixture mascot",
  "authority": { "locked_by": "Gary", "locked_on": "2026-07-10" },
  "identity": {
    "form": "a small orange test mascot with a round face",
    "hard_rules": ["always has exactly three freckles"]
  },
  "references": { "primary": "front.png", "turnaround": "turnaround.png" },
  "angles": { "front": ["primary"], "back": ["turnaround"] },
  "wardrobe": {
    "default": "everyday",
    "eras": { "everyday": { "desc": "blue overalls with a brass button" } }
  },
  "verification": ["crop-zoom face: three freckles present"],
  "never": ["a fourth freckle"]
}
```

Write `test-fixtures/canon/broken-entity/manifest.json` (three deliberate errors: bad type, missing reference file, undeclared angle role — and no `identity`):

```json
{
  "slug": "broken-entity",
  "type": "creature",
  "name": "Broken",
  "authority": { "locked_by": "Gary", "locked_on": "2026-07-10" },
  "references": { "primary": "missing.png" },
  "angles": { "front": ["primary", "ghost-role"] }
}
```

- [ ] **Step 2: Write the failing test** — `scripts/canon.test.mjs`:

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {join, dirname} from 'node:path';
import {fileURLToPath} from 'node:url';
import {discoverEntities, validateManifest} from './canon.mjs';

const FIX = join(dirname(fileURLToPath(import.meta.url)), 'test-fixtures', 'canon');

test('discoverEntities finds manifest folders, sorted', () => {
  const entities = discoverEntities(FIX);
  assert.deepEqual(entities.map((e) => e.slug), ['broken-entity', 'valid-person']);
  assert.equal(entities[1].manifest.name, 'Testy');
});

test('discoverEntities returns [] for a missing root', () => {
  assert.deepEqual(discoverEntities(join(FIX, 'nope')), []);
});

test('valid manifest produces no errors', () => {
  const e = discoverEntities(FIX).find((x) => x.slug === 'valid-person');
  assert.deepEqual(validateManifest(e), []);
});

test('broken manifest reports each defect with its slug', () => {
  const e = discoverEntities(FIX).find((x) => x.slug === 'broken-entity');
  const errors = validateManifest(e);
  assert.ok(errors.some((m) => m.includes('type must be one of person|place|prop')));
  assert.ok(errors.some((m) => m.includes('identity.form is required')));
  assert.ok(errors.some((m) => m.includes('missing file "missing.png"')));
  assert.ok(errors.some((m) => m.includes('undeclared reference role "ghost-role"')));
  assert.ok(errors.every((m) => m.startsWith('broken-entity:')));
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `cd ~/Documents/github-repos/supersuit-repos/brand-os-template && node --test scripts/`
Expected: FAIL — `Cannot find module ... canon.mjs`

- [ ] **Step 4: Implement `scripts/canon.mjs` (discovery + validation half)**

```js
// Canon OS — discovery, validation, and compilation of entity dossiers.
// Stdlib only: this runs wherever build-brand-txt.mjs runs.
// Schema doc: scripts/canon-manifest.schema.json (Task 2 sibling doc).
import {readFileSync, readdirSync, statSync, existsSync} from 'node:fs';
import {join} from 'node:path';

const TYPES = ['person', 'place', 'prop'];

export function discoverEntities(canonRootAbs) {
  if (!existsSync(canonRootAbs)) return [];
  const out = [];
  for (const name of readdirSync(canonRootAbs).sort()) {
    const dir = join(canonRootAbs, name);
    if (!statSync(dir).isDirectory()) continue;
    const mf = join(dir, 'manifest.json');
    if (!existsSync(mf)) continue;
    try {
      out.push({dir, slug: name, manifest: JSON.parse(readFileSync(mf, 'utf8')), parseError: null});
    } catch (e) {
      out.push({dir, slug: name, manifest: null, parseError: e.message});
    }
  }
  return out;
}

export function validateManifest(entity) {
  const {manifest: m, dir, slug, parseError} = entity;
  if (parseError) return [`${slug}: manifest.json is not valid JSON (${parseError})`];
  const errors = [];
  const need = (cond, msg) => { if (!cond) errors.push(`${slug}: ${msg}`); };
  need(m.slug === slug, `manifest slug "${m.slug}" must match folder name "${slug}"`);
  need(TYPES.includes(m.type), `type must be one of ${TYPES.join('|')} (got "${m.type}")`);
  need(typeof m.name === 'string' && m.name.length > 0, 'name is required');
  need(!!(m.authority && m.authority.locked_by && m.authority.locked_on),
    'authority.locked_by and authority.locked_on are required');
  need(!!(m.identity && typeof m.identity.form === 'string' && m.identity.form.length > 0),
    'identity.form is required');
  const refs = m.references || {};
  need(Object.keys(refs).length > 0, 'at least one entry in references is required');
  for (const [role, file] of Object.entries(refs)) {
    if (!existsSync(join(dir, file))) errors.push(`${slug}: references.${role} names missing file "${file}"`);
  }
  for (const [angle, roles] of Object.entries(m.angles || {})) {
    for (const role of roles || []) {
      if (!(role in refs)) errors.push(`${slug}: angles.${angle} names undeclared reference role "${role}"`);
    }
  }
  if (m.type === 'person' && m.wardrobe) {
    need(!!(m.wardrobe.default && m.wardrobe.eras && m.wardrobe.eras[m.wardrobe.default]),
      'wardrobe.default must name a key in wardrobe.eras');
  }
  return errors;
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `node --test scripts/`
Expected: all tests PASS

- [ ] **Step 6: Add test script to package.json**

In `package.json` `"scripts"`, after `"brand-txt": "node scripts/build-brand-txt.mjs",` add:

```json
"test": "node --test scripts/",
```

Run: `pnpm run test` — Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add scripts/canon.mjs scripts/canon.test.mjs scripts/test-fixtures package.json
git commit -m "feat(canon-os): entity discovery + manifest validation

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: `compileEntity` — descriptor compilation + schema doc

**Files:**
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon.mjs` (append function)
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon.test.mjs` (append tests)
- Create: `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon-manifest.schema.json`

**Interfaces:**
- Consumes: `discoverEntities` fixtures from Task 1.
- Produces: `compileEntity(entity, urlFn: (relFromRepo: string) => string, relDirFromRepo: string) -> string[]` (brand.txt lines, last element `''`).

- [ ] **Step 1: Write the failing tests** — append to `scripts/canon.test.mjs`:

```js
import {compileEntity} from './canon.mjs';

test('compileEntity emits header, per-era descriptors, PASS, VERIFY', () => {
  const e = discoverEntities(FIX).find((x) => x.slug === 'valid-person');
  const lines = compileEntity(e, (rel) => `https://x.test/${rel}`, 'static/brand/canon/valid-person');
  const text = lines.join('\n');
  assert.ok(lines[0].startsWith('### Testy (person) — locked by Gary 2026-07-10'));
  assert.ok(text.includes('DESCRIPTOR (everyday): a small orange test mascot'));
  assert.ok(text.includes('always has exactly three freckles'));
  assert.ok(text.includes('blue overalls with a brass button'));
  assert.ok(text.includes('NEVER: a fourth freckle.'));
  assert.ok(text.includes('PASS front: https://x.test/static/brand/canon/valid-person/front.png'));
  assert.ok(text.includes('back: https://x.test/static/brand/canon/valid-person/turnaround.png'));
  assert.ok(text.includes('VERIFY ON READ-BACK: crop-zoom face: three freckles present'));
  assert.equal(lines[lines.length - 1], '');
});

test('compileEntity for a place folds geometry/population into one descriptor', () => {
  const e = {
    dir: '/tmp/x', slug: 'the-spot', parseError: null,
    manifest: {
      slug: 'the-spot', type: 'place', name: 'The Spot',
      authority: {locked_by: 'Gary', locked_on: '2026-07-10', origin: 'origin-book'},
      identity: {form: 'a porch with plank steps'},
      geometry: 'steps face the river; one hanging lamp',
      population: 'exactly four people unless the spread says otherwise',
      references: {}, angles: {},
    },
  };
  const lines = compileEntity(e, (rel) => rel, 'canon/the-spot');
  const text = lines.join('\n');
  assert.ok(lines[0].includes('origin: origin-book'));
  assert.ok(text.includes('DESCRIPTOR: a porch with plank steps. steps face the river; one hanging lamp. exactly four people unless the spread says otherwise.'));
});
```

- [ ] **Step 2: Run tests to verify the new ones fail**

Run: `node --test scripts/` — Expected: the two new tests FAIL (`compileEntity` not exported).

- [ ] **Step 3: Implement — append to `scripts/canon.mjs`:**

```js
export function compileEntity(entity, urlFn, relDirFromRepo) {
  const m = entity.manifest;
  const L = [];
  const auth = m.authority;
  L.push(`### ${m.name} (${m.type}) — locked by ${auth.locked_by} ${auth.locked_on}${auth.origin ? `, origin: ${auth.origin}` : ''}`);
  if (m.role) L.push(`Role: ${m.role}`);
  const base = [m.identity.form, ...((m.identity && m.identity.hard_rules) || [])];
  const never = (m.never || []).length ? ` NEVER: ${m.never.join('; ')}.` : '';
  const eras = m.type === 'person' && m.wardrobe && m.wardrobe.eras ? Object.entries(m.wardrobe.eras) : null;
  if (eras && eras.length) {
    for (const [era, e] of eras) {
      const scope = e.spreads ? `, spreads ${e.spreads}` : '';
      L.push(`DESCRIPTOR (${era}${scope}): ${base.join('. ')}. ${e.desc}.${never}`);
    }
  } else {
    const extra = [m.geometry, m.lighting, m.population, m.construction, m.scale].filter(Boolean);
    L.push(`DESCRIPTOR: ${[...base, ...extra].join('. ')}.${never}`);
  }
  const angleEntries = Object.entries(m.angles || {}).filter(([, roles]) => (roles || []).length);
  if (angleEntries.length) {
    const parts = angleEntries.map(([angle, roles]) =>
      `${angle}: ${roles.map((r) => urlFn(`${relDirFromRepo}/${m.references[r]}`)).join(', ')}`);
    L.push(`PASS ${parts.join(' · ')}`);
  }
  if ((m.verification || []).length) L.push(`VERIFY ON READ-BACK: ${m.verification.join(' · ')}`);
  if (m.voice && m.voice.narration_voice_id) {
    L.push(`NARRATION VOICE: ${m.voice.narration_voice_id}${m.voice.speech ? ` (${m.voice.speech})` : ''}`);
  }
  L.push('');
  return L;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test scripts/` — Expected: all PASS.

- [ ] **Step 5: Write the schema doc** — `scripts/canon-manifest.schema.json` (documentation the validator mirrors; not loaded at runtime):

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Canon OS entity manifest",
  "description": "One folder per recurring visual entity under static/brand/canon/<slug>/. The validator in canon.mjs enforces the required subset; this file is the authoritative field reference.",
  "type": "object",
  "required": ["slug", "type", "name", "authority", "identity", "references"],
  "properties": {
    "slug": { "type": "string", "description": "Must equal the folder name." },
    "type": { "enum": ["person", "place", "prop"] },
    "name": { "type": "string" },
    "role": { "type": "string" },
    "authority": {
      "type": "object",
      "required": ["locked_by", "locked_on"],
      "properties": {
        "locked_by": { "type": "string" },
        "locked_on": { "type": "string", "description": "YYYY-MM-DD" },
        "origin": { "type": "string", "description": "Brand/book where canon originates, for imported entities." },
        "note": { "type": "string" }
      }
    },
    "identity": {
      "type": "object",
      "required": ["form"],
      "properties": {
        "form": { "type": "string", "description": "Prose visual description; first sentence of every compiled descriptor." },
        "hard_rules": { "type": "array", "items": { "type": "string" } }
      }
    },
    "references": {
      "type": "object",
      "description": "role -> filename co-located in this folder. Conventional roles: primary, face, turnaround, back_detail, expressions.",
      "additionalProperties": { "type": "string" }
    },
    "angles": {
      "type": "object",
      "description": "view -> list of reference roles that MUST be passed for that view (front/back/side/...).",
      "additionalProperties": { "type": "array", "items": { "type": "string" } }
    },
    "wardrobe": {
      "type": "object",
      "description": "person only. The face never changes era to era; only wardrobe and age markers do.",
      "properties": {
        "default": { "type": "string" },
        "eras": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "required": ["desc"],
            "properties": { "spreads": { "type": "string" }, "desc": { "type": "string" } }
          }
        }
      }
    },
    "voice": {
      "type": "object",
      "description": "person only.",
      "properties": {
        "speech": { "type": "string" },
        "narration_voice_id": { "type": "string", "description": "ElevenLabs voice ID." }
      }
    },
    "geometry": { "type": "string", "description": "place only: locked spatial description." },
    "lighting": { "type": "string", "description": "place only." },
    "population": { "type": "string", "description": "place only, e.g. 'exactly four people unless the spread says otherwise'." },
    "construction": { "type": "string", "description": "prop only: locked build description." },
    "scale": { "type": "string", "description": "prop only: relation to figures." },
    "verification": { "type": "array", "items": { "type": "string" }, "description": "Machine-readable read-back checklist (crop-zoom targets)." },
    "never": { "type": "array", "items": { "type": "string" }, "description": "Drift blacklist." }
  }
}
```

- [ ] **Step 6: Commit**

```bash
git add scripts/canon.mjs scripts/canon.test.mjs scripts/canon-manifest.schema.json
git commit -m "feat(canon-os): descriptor compiler + manifest schema doc

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Wire Canon OS into `build-brand-txt.mjs` + starter entity

**Files:**
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/build-brand-txt.mjs` (import at top; section after the Characters block, i.e. after current line 136)
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/brand.json` (add `canon` key)
- Create: `~/Documents/github-repos/supersuit-repos/brand-os-template/static/brand/canon/example-mascot/` (manifest + placeholder image)

**Interfaces:**
- Consumes: `discoverEntities`, `validateManifest`, `compileEntity` from `scripts/canon.mjs` (Tasks 1–2).
- Produces: brand.txt section `## Canon (compiled descriptors — paste verbatim, never paraphrase)`; build exits 1 on any validation error.

- [ ] **Step 1: Add the import** — top of `build-brand-txt.mjs`, after the existing imports (line 15):

```js
import {discoverEntities, validateManifest, compileEntity} from './canon.mjs';
```

- [ ] **Step 2: Add the section** — in `build-brand-txt.mjs`, immediately after the Characters section's closing `}` (current line 136), insert:

```js
// Canon OS section — entity dossiers compiled from canon/<slug>/manifest.json.
// Spec: garyinparadise/docs/strategic-documents/2026-07-10-canon-os-design.md
if (bj.canon?.root) {
  const canonAbs = join(REPO, bj.canon.root.replace(/\/+$/, ''));
  const entities = discoverEntities(canonAbs);
  const canonErrors = entities.flatMap((e) => validateManifest(e));
  if (canonErrors.length) {
    console.error('[brand.txt] CANON VALIDATION FAILED — fix the manifests below (the build is halted so a broken dossier can never ship):');
    for (const err of canonErrors) console.error('  - ' + err);
    process.exit(1);
  }
  if (entities.length) {
    A('## Canon (compiled descriptors — paste verbatim, never paraphrase)');
    A('Every recurring visual entity (person/place/prop). The DESCRIPTOR lines are compiled from the entity manifests: paste them into prompts verbatim, pass every PASS url for the view you are rendering, and run the VERIFY checklist on every read-back. A render that disagrees with a dossier is a defective render; only the locking authority relocks a dossier.');
    A('');
    for (const e of entities) {
      for (const line of compileEntity(e, url, relative(REPO, e.dir))) A(line);
    }
  }
}
```

- [ ] **Step 3: Add `canon` key to template brand.json** — after the `"characters"` block's closing brace, add:

```json
"canon": { "root": "static/brand/canon/" },
```

- [ ] **Step 4: Create the starter entity**

```bash
mkdir -p static/brand/canon/example-mascot
# 1x1 transparent PNG placeholder (replace with the real mascot sheet when forking)
printf 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPhfDwAChwGA60e6kgAAAABJRU5ErkJggg==' | base64 -d > static/brand/canon/example-mascot/front.png
```

Write `static/brand/canon/example-mascot/manifest.json`:

```json
{
  "slug": "example-mascot",
  "type": "person",
  "name": "{{Mascot Name}}",
  "role": "{{brand mascot / spokesperson — delete this folder or make it real when forking}}",
  "authority": { "locked_by": "{{owner}}", "locked_on": "{{YYYY-MM-DD}}" },
  "identity": {
    "form": "{{prose visual description — species/build/face/palette}}",
    "hard_rules": ["{{the one detail that must never drift, e.g. garment mark, eye rule}}"]
  },
  "references": { "primary": "front.png" },
  "angles": { "front": ["primary"] },
  "wardrobe": {
    "default": "everyday",
    "eras": { "everyday": { "desc": "{{signature outfit incl. exact patch/mark placement}}" } }
  },
  "verification": ["{{crop-zoom target: what to check on every read-back}}"],
  "never": ["{{drift blacklist: logos, extra accessories, redesigns}}"]
}
```

- [ ] **Step 5: Smoke-test the build**

Run: `node scripts/build-brand-txt.mjs`
Expected: exit 0; `grep -A3 '## Canon' static/brand.txt` shows the section with `### {{Mascot Name}} (person)`.

Then prove the failure path: temporarily rename `front.png` to `front2.png`, run again — Expected: exit 1 with `example-mascot: references.primary names missing file "front.png"`. Rename it back, rerun, exit 0.

- [ ] **Step 6: Run the full test suite**

Run: `pnpm run test` — Expected: PASS (canon tests unaffected by wiring).

- [ ] **Step 7: Commit**

```bash
git add scripts/build-brand-txt.mjs brand.json static/brand/canon/
git commit -m "feat(canon-os): compile canon dossiers into brand.txt; example-mascot starter

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Template docs

**Files:**
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/GETTING-STARTED.md`
- Modify: `~/Documents/github-repos/supersuit-repos/brand-os-template/AGENTS.md`

**Interfaces:**
- Consumes: the layout and schema from Tasks 1–3 (docs must match `canon-manifest.schema.json` field names exactly).

- [ ] **Step 1: Add a "Canon OS" section to GETTING-STARTED.md** (after whatever section introduces GABRs; read the file first and match its heading style):

```markdown
## Canon OS: entity dossiers (characters, mascots, places, props)

Any recurring visual entity gets a folder under `static/brand/canon/<slug>/`:
`manifest.json` plus that entity's reference images (front, face, turnaround,
back detail). The build validates every manifest (missing fields or files fail
the build) and compiles each one into a paste-verbatim DESCRIPTOR + required
PASS refs per angle + a VERIFY read-back checklist in brand.txt.

Rules the schema encodes:
- **The dossier is the source of truth.** A render that disagrees with it is a
  defective render. Only the `authority.locked_by` person relocks a dossier.
- **Multi-angle always.** If an entity will ever be seen from behind or the
  side, lock those references before production and map them in `angles`.
- **Descriptors are compiled, never hand-written.** Paste from brand.txt.

Field reference: `scripts/canon-manifest.schema.json`. Starter:
`static/brand/canon/example-mascot/` (make it real or delete it).
The old flat `characters` block in brand.json still works; canon/ is where new
entities go.
```

- [ ] **Step 2: Add two lines to AGENTS.md** (in whatever section lists the build outputs; read first, match style): note that `scripts/canon.mjs` is part of the canonical build machinery maintained by the `update-brand-os-standard` skill, and that `canon/` dossiers are validated at build time.

- [ ] **Step 3: Verify + commit**

Run: `node scripts/build-brand-txt.mjs` — Expected: exit 0 (docs changes can't break it; this is the habit).

```bash
git add GETTING-STARTED.md AGENTS.md
git commit -m "docs(canon-os): dossier guide in GETTING-STARTED + AGENTS notes

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Skill updates (no git — `~/.claude/skills/` is not a repo)

**Files:**
- Modify: `~/.claude/skills/update-brand-os-standard/SKILL.md`
- Modify: `~/.claude/skills/generate-a-brand-os/SKILL.md`
- Modify: `~/.claude/skills/create-brand-os-picture-book/SKILL.md`

**Interfaces:**
- Consumes: section heading + file names from Task 3 (`## Canon (compiled descriptors — paste verbatim, never paraphrase)`, `scripts/canon.mjs`, `scripts/canon-manifest.schema.json`).

- [ ] **Step 1: update-brand-os-standard** — read the SKILL.md, find where it enumerates the standard brand.txt sections emitted by `build-brand-txt.mjs`, and add the Canon section to that list with one line:

```markdown
- **Canon (compiled descriptors)** — entity dossiers from `static/brand/canon/<slug>/manifest.json`, validated at build time (`scripts/canon.mjs` + `scripts/canon-manifest.schema.json`); emits paste-verbatim DESCRIPTOR/PASS/VERIFY blocks. Added 2026-07-10.
```

Also add `scripts/canon.mjs` and `scripts/canon-manifest.schema.json` wherever the skill lists the canonical files to copy when updating a brand to the standard.

- [ ] **Step 2: generate-a-brand-os** — in the local SKILL.md's Step 0 (fork-the-template) section, add one sentence: new brands define recurring entities as `canon/` dossiers (see the template's GETTING-STARTED Canon OS section), not only flat `characters` entries.

- [ ] **Step 3: create-brand-os-picture-book** — in the stage-2 GABR section (the MULTI-ANGLE TURNAROUNDS and GABR-IS-THE-SOURCE-OF-TRUTH blocks added 2026-07-10), add the pointer:

```markdown
**CANON OS (2026-07-10):** new books define each recurring character, place, and prop as a `canon/<slug>/` dossier (manifest + co-located references) per the brand-os-template schema. The build compiles paste-verbatim descriptors into brand.txt — batch scripts paste those, never hand-write `$CHARACTER` strings (hand-maintained descriptors are how the flag/daisy drift happened). Turnarounds map to `angles`, Gary's lock lives in `authority`, and the crop-zoom checks live in `verification`.
```

- [ ] **Step 4: Verify** — no build to run; re-read each edited section for contradiction with the older GABR text (older text must read as "still valid, superseded for new work by canon/").

---

### Task 6: Wheel-and-oar retrofit (worked example)

**Files:**
- Modify: `~/Documents/github-repos/nation-of-fire/wheel-and-oar-book/brand-os/scripts/build-brand-txt.mjs` (replace with template copy) + Create: `.../brand-os/scripts/canon.mjs` (copy)
- Modify: `~/Documents/github-repos/nation-of-fire/wheel-and-oar-book/brand-os/brand.json` (add `canon` key)
- Create: `.../brand-os/static/brand/canon/{jerry-man,the-boat,the-porch}/` (manifests + image copies)

**Interfaces:**
- Consumes: canonical scripts from Task 3. GABR source files (all under `.../brand-os/static/brand/generation-layer/golden-atomic-brand-references/`): `gabr-02-jerry-man.png`, `gabr-03-jerry-man-face.png`, `gabr-18-jerry-turnaround.png`, `gabr-12-jerry-jacket-back.png`, `gabr-07-the-boat.png`, `gabr-08-the-porch.png`.
- Produces: the acceptance test — the compiled Jerry descriptor reproduces the corrected canon with no hand-editing.

- [ ] **Step 1: Copy the canonical scripts**

```bash
cd ~/Documents/github-repos/nation-of-fire/wheel-and-oar-book/brand-os
cp ~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon.mjs scripts/
cp ~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/build-brand-txt.mjs scripts/
diff scripts/build-brand-txt.mjs ~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/build-brand-txt.mjs
```

Expected diff: none. (The reuse-before-rebuild rule: the book's build script must BE the canonical one.)

- [ ] **Step 2: Create the three entity folders and copy references**

```bash
G=static/brand/generation-layer/golden-atomic-brand-references
mkdir -p static/brand/canon/{jerry-man,the-boat,the-porch}
cp $G/gabr-02-jerry-man.png        static/brand/canon/jerry-man/front.png
cp $G/gabr-03-jerry-man-face.png   static/brand/canon/jerry-man/face.png
cp $G/gabr-18-jerry-turnaround.png static/brand/canon/jerry-man/turnaround.png
cp $G/gabr-12-jerry-jacket-back.png static/brand/canon/jerry-man/jacket-back.png
cp $G/gabr-07-the-boat.png         static/brand/canon/the-boat/emblem.png
cp $G/gabr-08-the-porch.png        static/brand/canon/the-porch/plate.png
```

(If Gary has not yet approved `gabr-18-jerry-turnaround.png`, pause this task at this step and get the approval — a dossier must not lock an unapproved sheet.)

- [ ] **Step 3: Write `static/brand/canon/jerry-man/manifest.json`**

```json
{
  "slug": "jerry-man",
  "type": "person",
  "name": "Jerry",
  "role": "host and teacher; the universe's recurring everyman-believer",
  "authority": {
    "locked_by": "Gary",
    "locked_on": "2026-07-10",
    "origin": "golden-path-book",
    "note": "Only Gary relocks. A render that disagrees with this dossier is a defective render — fix the render, never the dossier."
  },
  "identity": {
    "form": "Chinese-American man, 33, handsome: strong clean jawline, short dark hair brushed up and back, clean-shaven, warm brown eyes, lean athletic build, about 7.5 heads tall",
    "hard_rules": [
      "DOUBLE EYELIDS: a clear inked crease line above each eye, in every image, at every scale; smiles, squints, and strain do not erase it",
      "his ONLY necklace is the Liberty pendant (large-coin size, never a medallion)",
      "faces verified at crop scale on every read-back"
    ]
  },
  "references": {
    "primary": "front.png",
    "face": "face.png",
    "turnaround": "turnaround.png",
    "back_detail": "jacket-back.png"
  },
  "angles": {
    "front": ["primary", "face"],
    "side": ["turnaround", "face"],
    "back": ["turnaround", "back_detail"]
  },
  "wardrobe": {
    "default": "present-day",
    "eras": {
      "present-day": {
        "spreads": "1-5, 12-33",
        "desc": "light-wash denim trucker jacket whose chest carries EXACTLY the two small patches from the front sheet: a small yellow smiley on the wearer's right chest and a small rectangular Texas-flag patch (vertical blue band with one white star, white bar over red bar) on the wearer's left chest, both just above the pocket flaps, same small size; the jacket's UPPER BACK carries the old-gold NORTH STAR CROSS (per the back sheet) in every view of his back; plain white heavyweight tee; Liberty pendant; relaxed light-wash jeans; plain cherry-red-and-white sneakers with NO logo, NO swoosh, NO brand marks"
      },
      "flashback": {
        "spreads": "6-11",
        "desc": "pre-conversion: plainer greyer clothing, NO jacket mark, NO patches, NO pendant, NO necklace of any kind; same face, same double eyelids"
      }
    }
  },
  "voice": {
    "speech": "plain-spoken, kind, testimony-first; no jargon, no em dashes",
    "narration_voice_id": "r1KmysJdVYZjJCm4mL3b"
  },
  "verification": [
    "crop-zoom eyes: double-eyelid crease present above each eye",
    "patches match the front sheet: smiley wearer's-right, Texas flag wearer's-left, same small size, just above the pocket flaps, nothing else on the front",
    "any back view: North Star Cross in old gold on the upper back; front patches NEVER appear on the back",
    "shoes completely unbranded (no swoosh)",
    "flashback spreads: no pendant, no patches, no mark"
  ],
  "never": ["Nike swoosh or any brand logo", "a third patch", "patch redesign or resize", "a beard", "any necklace but the Liberty pendant"]
}
```

- [ ] **Step 4: Write `static/brand/canon/the-boat/manifest.json`**

```json
{
  "slug": "the-boat",
  "type": "prop",
  "name": "The Boat",
  "role": "the book's emblem: every believer's heaven-built vessel",
  "authority": {
    "locked_by": "Gary",
    "locked_on": "2026-07-10",
    "note": "Locked as gabr-07 stern-a by Gary. Only Gary relocks."
  },
  "identity": {
    "form": "a heaven-built ship clearly not from any earthly boatyard: pale pearl-ivory grown-wood hull with a soft moonlit inner sheen, planks flowing in long seamless curves, impossibly graceful upswept prow whose tip curls into a small scroll (fern-frond volute, the Helper's place), thin starlight-white inlay tracing the gunwale, resting feather-light on the water",
    "hard_rules": [
      "full multi-handled captain's wheel of warm turned wood on a carved helm post at the BOW, beautifully too grand for so small a craft, NO seat at the helm",
      "short EMPTY Captain's plank thwart one step behind the wheel (the steerer's seat belongs to the Lord)",
      "generous open floor, then ONE rower's bench; the LONGER stern bench connects into the stern",
      "a matched PAIR of plain ordinary wooden oars in SCULLING position: oarlocks directly opposite each other at the rower's bench, one rower pulls both",
      "GOLD falls on the wheel only, NEVER on the oars",
      "ONE PERSON, ONE BOAT: never two people in one boat; in multi-boat scenes each vessel is the same type but visibly its owner's own",
      "no sail, no motor, never chrome, never sci-fi"
    ]
  },
  "references": { "primary": "emblem.png" },
  "angles": { "front": ["primary"], "side": ["primary"], "back": ["primary"] },
  "construction": "wheel at bow, empty Captain's thwart behind it, open floor, single rower's bench with sculling oar pair, long stern bench joined to the stern",
  "scale": "small craft for one rower; the wheel reads oversized for the hull",
  "verification": [
    "scroll curl present at the prow tip",
    "wheel at the bow with NO seat; empty plank thwart behind it",
    "TWO oars, oarlocks opposite each other at one station",
    "gold on wheel only, oars plain wood",
    "one person maximum aboard"
  ],
  "never": ["a second rower", "a chair at the helm", "staggered two-man oar stations", "gold on the oars", "an earthly plank-and-nail hull"]
}
```

- [ ] **Step 5: Write `static/brand/canon/the-porch/manifest.json`**

```json
{
  "slug": "the-porch",
  "type": "place",
  "name": "The Porch",
  "role": "the book's home base: where Jerry teaches his friends at dusk",
  "authority": { "locked_by": "Gary", "locked_on": "2026-07-10" },
  "identity": {
    "form": "a warm wooden cabin porch above the river at dusk: plank steps, sturdy rail, one hanging cream lamp, lemonade on a small table, wildflowers below, the river and mooring post beneath, the pearl ship moored"
  },
  "references": { "primary": "plate.png" },
  "angles": { "front": ["primary"] },
  "geometry": "steps descend toward the riverbank; rail on the river side; lamp hangs by the door; table with lemonade pitcher and glasses on the porch",
  "lighting": "quiet pale cream lamplight (#F2E3C0), clearly dimmer and cooler than sacred gold; indigo dusk sky (#3E4A6B)",
  "population": "EXACTLY four people (Jerry, Dre, Hana, Theo) unless the spread says otherwise; no extra figures, no duplicates",
  "verification": [
    "head count: exactly four unless the spread's bracket says otherwise",
    "one hanging cream lamp, not radiant gold",
    "porch geometry matches the plate (steps, rail, table, mooring post below)"
  ],
  "never": ["extra or duplicate figures", "a second lamp", "sacred gold in the lamplight"]
}
```

- [ ] **Step 6: Add the canon key to the book's brand.json**

In `brand-os/brand.json`, after the `"characters"` block's closing brace, add:

```json
"canon": { "root": "static/brand/canon/" },
```

- [ ] **Step 7: Build and run the acceptance test**

```bash
node scripts/build-brand-txt.mjs
grep -A6 '### Jerry (person)' static/brand.txt
```

Expected: exit 0. The Jerry block must contain — with no hand-editing of brand.txt — "Texas-flag patch", "NORTH STAR CROSS", "DOUBLE EYELIDS", "NO swoosh", both eras (present-day, flashback spreads 6-11), PASS lines for front/side/back, and the VERIFY checklist. If any is missing, fix the MANIFEST and rebuild (never the output).

- [ ] **Step 8: Commit (no co-author trailer — Nation of Fire repo rule)**

```bash
cd ~/Documents/github-repos/nation-of-fire/wheel-and-oar-book
git add brand-os/
git commit -m "Canon OS retrofit: jerry-man, the-boat, the-porch dossiers; canonical build scripts"
```

---

### Task 7: Universe origin-pointers

**Files:**
- Modify: `~/Documents/github-repos/nation-of-fire/universe/brand-os/scripts/build-brand-txt.mjs` (replace with template copy) + Create: `.../brand-os/scripts/canon.mjs` (copy)
- Modify: `~/Documents/github-repos/nation-of-fire/universe/brand-os/brand.json` (add `canon` key)
- Create: `.../brand-os/static/brand/canon/{jerry-man,the-wisp}/` manifests (+ image copies from the universe GABR folder)

**Interfaces:**
- Consumes: canonical scripts (Task 3); universe GABR copies at `.../brand-os/static/brand/generation-layer/golden-atomic-brand-references/` (`gabr-02-jerry-man.png`, `gabr-01-the-wisp.png`).
- Produces: universe brand.txt Canon section where every imported entity names its origin. (The dark council's three dossiers follow the same pattern from case-777316 GABRs — do them in this task if the council GABR copies exist in the universe folder; otherwise log them as the first opportunistic migration in universe/CANON.md rather than blocking.)

- [ ] **Step 1: Copy scripts, create folders, copy references**

```bash
cd ~/Documents/github-repos/nation-of-fire/universe/brand-os
cp ~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/canon.mjs scripts/
cp ~/Documents/github-repos/supersuit-repos/brand-os-template/scripts/build-brand-txt.mjs scripts/
G=static/brand/generation-layer/golden-atomic-brand-references
mkdir -p static/brand/canon/{jerry-man,the-wisp}
cp $G/gabr-02-jerry-man.png static/brand/canon/jerry-man/front.png
cp $G/gabr-01-the-wisp.png  static/brand/canon/the-wisp/primary.png
```

- [ ] **Step 2: Write `static/brand/canon/jerry-man/manifest.json`** — same content as Task 6 Step 3 EXCEPT: `references` is only `{ "primary": "front.png" }`, `angles` is only `{ "front": ["primary"] }`, and `authority.note` becomes: "Origin canon lives in golden-path-book; book-level dossiers (e.g. wheel-and-oar-book) carry the angle-complete reference sets. Only Gary relocks." All other fields (identity, wardrobe eras minus the spreads keys, voice, verification, never) are copied verbatim from Task 6 Step 3 — the `spreads` values are book-specific, so DELETE the `spreads` keys in the universe copy.

- [ ] **Step 3: Write `static/brand/canon/the-wisp/manifest.json`**

```json
{
  "slug": "the-wisp",
  "type": "person",
  "name": "The Wisp (the Helper)",
  "role": "the Holy Spirit made visible; identical in every property",
  "authority": {
    "locked_by": "Gary",
    "locked_on": "2026-07-10",
    "origin": "golden-path-book",
    "note": "Universe-wide canon. Only Gary relocks."
  },
  "identity": {
    "form": "one small upright candle-flame of warm golden light: rounded base, gentle taper to a soft point, one lively curve, bright white-gold core, amber edges, soft round halo",
    "hard_rules": [
      "FACELESS: no face, no limbs, never cute-ified",
      "never a comet, tadpole, teardrop-with-tail, or sperm shape; no droplets",
      "in walking or rowing scenes He floats directly AHEAD of the walker on that walker's own path; He leads, never trails",
      "gold belongs to God: the wisp's radiant gold never touches a devil"
    ]
  },
  "references": { "primary": "primary.png" },
  "angles": { "front": ["primary"] },
  "verification": [
    "upright flame silhouette (rounded base, soft taper), not a comet or teardrop",
    "no face, no limbs",
    "positioned ahead of the walker on the walker's own path",
    "small relative to figures (fist-sized), never towering"
  ],
  "never": ["a face", "limbs", "a tail", "cuteness", "leading someone else's path"]
}
```

- [ ] **Step 4: Add `"canon": { "root": "static/brand/canon/" },` to the universe brand.json** (after the `"characters"` block), then build:

```bash
node scripts/build-brand-txt.mjs
grep 'origin: golden-path-book' static/brand.txt
```

Expected: exit 0; both entity headers carry `origin: golden-path-book`.

- [ ] **Step 5: Commit and push (no co-author trailer)**

```bash
cd ~/Documents/github-repos/nation-of-fire/universe
git add brand-os/
git commit -m "Canon OS: jerry-man + the-wisp dossiers with origin pointers; canonical build scripts"
git push
```

---

## Self-review notes (run after drafting — issues found and fixed inline)

- Spec coverage: layout (T3), schema (T2), validation (T1), compiled descriptors (T2/T3), backward compat (T3 leaves characters emitter untouched), template starter (T3), docs (T4), skills (T5), wheel-and-oar retrofit (T6), universe origin pointers (T7). Verify-helper explicitly out of scope per spec.
- Type consistency: `discoverEntities/validateManifest/compileEntity` signatures match across T1→T3; section heading string identical in T3/T5.
- The dark council dossiers are conditionally in T7 (spec lists them; the universe folder may lack case-777316 GABR copies — the task says log as first opportunistic migration rather than block).
